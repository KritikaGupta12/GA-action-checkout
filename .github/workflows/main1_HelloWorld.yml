on: 
  workflow_dispatch: 
jobs:
  display_files:
    name: run a sample command
    runs-on: ubuntu-latest
    steps:        
      - name: display output
        run: |
          echo "Hello World"
        shell: bash
