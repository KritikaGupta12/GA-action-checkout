name: Schedule the workflow

on:
  schedule:
    # - cron: '30 7 * * *'
    - cron: '*/5 * * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Run a script
        run: echo "This is a scheduled action!"
